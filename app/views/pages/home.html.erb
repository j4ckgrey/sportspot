<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= image_path("1.jpg") %>);">
  <div class="container">
    <h1><strong>SportSpot brings your Sports booking with just few clicks</strong>!</h1>
    <p>Book, Play and Conquer!</p>
  </div>
</div>
<div class="container">
  <div class="row mt-5 mb-5 d-flex justify-content-center">
    <div class="col-sm-6 p-3">
      <div class="card bg-light">
        <%= form_tag(search_path, method: :get) do %>
              <%= text_field_tag :query, params[:query], placeholder: "Your post code"%>
              <%= select_tag :category, options_for_select(["All", "Football", "MartialArts", "Bouldering", "Kegel", "Bowling", "Volleyball"])%>
              <%= submit_tag "Search"%>
          <% end %>
        <div class="border border-primary mt-3">
          <div style="width: 100%; height: 350px;"
            data-controller="map"
            data-map-markers-value="<%= @markers.to_json %>"
            data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
          </div>
        </div>
      </div>
    </div>
    <div class="col-sm-6 p-3">
      <div class="card bg-light">
        <h1>Newsletter</h1>
        <ul>
          <li>This is newsletter from a certain Club .....</li>
          <li>This is newsletter from a certain Club .....</li>
          <li>This is newsletter from a certain Club .....</li>
          <li>This is newsletter from a certain Club .....</li>
          <li>This is newsletter from a certain Club .....</li>
          <li>This is newsletter from a certain Club .....</li>
        </ul>
      </div>
    </div>
  </div>
</div>
