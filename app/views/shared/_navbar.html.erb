<div class="navbar navbar-expand-sm navbar-light navbar-lewagon" style="background-color:#9cbfdd">
  <div class="container-fluid">
    <%= link_to "/", class: "navbar-brand fa-solid fa-volleyball btn-hover" do %>
    <% end %>
    <div class="d-flex ">
 <%= form_tag search_path, method: :get do %>
      <div class="row justify-content-center align-items-center">
        <div class="col-lg-12 col-md-12 col-sm-16">
          <div class="input-group our-text">
            <%= text_field_tag :query, params[:query], class: "form-control rounded-start me-2", placeholder: "city or post-code" %>
            <%= select_tag :category, options_for_select(['All', "Football", "MartialArts", "Bouldering", "Kegel", "Bowling", "Volleyball", "Squash"], @category), prompt: "Select a category", class: "form-select rounded-end me-2" %>
            <%= submit_tag "Search", class: "rounded search-button bg-success bg-gradient text-white" %>
          </div>
        </div>
      </div>
    <% end %>
    </div>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
        <% if user_signed_in? %>
          <li class="nav-item dropdown">
          <a href="#" class="avatar" id="navbarDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <% if current_user.photo.present? %>
              <%= cl_image_tag current_user.photo.key, class:"avatar", alt: "dropdown menu"%>
            <% else %>
              <%= image_tag "acc_default.png", class: "avatar", alt: "dropdown menu" %>
            <% end %>
          </a>
            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <p class="fs-7 d-block p-2 text-dark"><%= current_user.first_name %></p>
              <%= link_to "All venues", venues_path, class: "dropdown-item border d-block p-2 bg-secondary text-white" %>
              <%= link_to "All clubs", clubs_path, class: "dropdown-item border d-block p-2 bg-secondary text-white" %>
              <%= link_to "Dashboard", dashboard_path, class: "dropdown-item border d-block p-2 bg-secondary text-white" %>
              <%= link_to "Messages", chatrooms_path, class: "dropdown-item border d-block p-2 bg-secondary text-white" %>
              <%= link_to "Log out", destroy_user_session_path, data: { turbo_method: :delete }, class: "dropdown-item border d-block p-2 bg-secondary text-white" %>
            </div>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
